* build an Observable<Response> from scratch

        var observableResponse = new Observable<Response>(observer => {
            var options = new ResponseOptions( {
                body: '{"body": "double"}',
            } );

            let response:Response = new Response( options );
            response.ok = true;

            observer.next(response);

            observer.complete();
        });
        
        return observableResponse;