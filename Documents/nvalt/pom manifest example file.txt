<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
            <modelVersion>4.0.0</modelVersion>
            <parent>
                <groupId>org.ihc.hwcir</groupId>
                <artifactId>hwcir-pom</artifactId>
                <version>1.1.2</version>
            </parent>
            <artifactId>hwcir-java-pom</artifactId>
            <name>HWCIR Java POM</name>
            <url>http://maven.apache.org</url>
            <packaging>pom</packaging>
            <properties>
                    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
                    <build.manifest.section>Build Details</build.manifest.section>
                    <implementation.manifest.section>Implementation Details</implementation.manifest.section>
                    <build.unknown>UNKNOWN/LOCAL</build.unknown>
                    <build.number>${build.unknown}</build.number>
                    <build.revision>${build.unknown}</build.revision>
            </properties>
            <dependencies>
                    <dependency>
                        <groupId>junit</groupId>
                        <artifactId>junit</artifactId>
                    </dependency>
                    <dependency>
                        <groupId>org.jmock</groupId>
                        <artifactId>jmock-junit4</artifactId>
                    </dependency>
                    <dependency>
                        <groupId>log4j</groupId>
                        <artifactId>log4j</artifactId>
                    </dependency>
                    <dependency>
                        <groupId>org.apache.commons</groupId>
                        <artifactId>commons-lang3</artifactId>
                    </dependency>
                </dependencies>
                <build>
                    <plugins>
                            <plugin>
                                    <groupId>org.apache.maven.plugins</groupId>
                                    <artifactId>maven-jar-plugin</artifactId>
                                    <configuration>
                                            <archive>
                                                    <!-- To be passed in by the build system as -Dbuild.number=${BUILD_NUMBER} -Dbuild.revision=${GIT_COMMIT} -->
                                                    <manifestSections>
                                                        <manifestSection>
                                                            <name>${implementation.manifest.section}</name>
                                                            <manifestEntries>
                                                                <Implementation-Title>${project.name}</Implementation-Title>
                                                                <Implementation-Version>${project.version}</Implementation-Version>
                                                            </manifestEntries>
                                                        </manifestSection>
                                                        <manifestSection>
                                                            <name>${build.manifest.section}</name>
                                                            <manifestEntries>
                                                                <Build-Time>${maven.build.timestamp}</Build-Time>
                                                                <Build-Number>${build.number}</Build-Number>
                                                                <Build-Revision>${build.revision}</Build-Revision>
                                                            </manifestEntries>
                                                        </manifestSection>
                                                    </manifestSections>
                                            </archive>
                                    </configuration>
                            </plugin>
                            <plugin>
                                    <groupId>org.apache.maven.plugins</groupId>
                                    <artifactId>maven-war-plugin</artifactId>
                                    <configuration>
                                            <archive>
                                                <!-- To be passed in by the build system as -Dbuild.number=${BUILD_NUMBER} -Dbuild.revision=${GIT_COMMIT} -->
                                                <manifestSections>
                                                    <manifestSection>
                                                        <name>${implementation.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Implementation-Title>${project.name}</Implementation-Title>
                                                            <Implementation-Version>${project.version}</Implementation-Version>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                    <manifestSection>
                                                        <name>${build.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Build-Time>${maven.build.timestamp}</Build-Time>
                                                            <Build-Number>${build.number}</Build-Number>
                                                            <Build-Revision>${build.revision}</Build-Revision>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                </manifestSections>
                                            </archive>
                                    </configuration>
                            </plugin>
                            <plugin>
                                    <groupId>org.apache.maven.plugins</groupId>
                                    <artifactId>maven-ear-plugin</artifactId>
                                    <configuration>
                                            <archive>
                                                <!-- To be passed in by the build system as -Dbuild.number=${BUILD_NUMBER} -Dbuild.revision=${GIT_COMMIT} -->
                                                <manifestSections>
                                                    <manifestSection>
                                                        <name>${implementation.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Implementation-Title>${project.name}</Implementation-Title>
                                                            <Implementation-Version>${project.version}</Implementation-Version>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                    <manifestSection>
                                                        <name>${build.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Build-Time>${maven.build.timestamp}</Build-Time>
                                                            <Build-Number>${build.number}</Build-Number>
                                                            <Build-Revision>${build.revision}</Build-Revision>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                </manifestSections>
                                            </archive>
                                    </configuration>
                            </plugin>
                            <plugin>
                                    <groupId>org.apache.maven.plugins</groupId>
                                    <artifactId>maven-ejb-plugin</artifactId>
                                    <configuration>
                                            <archive>
                                                <!-- To be passed in by the build system as -Dbuild.number=${BUILD_NUMBER} -Dbuild.revision=${GIT_COMMIT} -->
                                                <manifestSections>
                                                    <manifestSection>
                                                        <name>${implementation.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Implementation-Title>${project.name}</Implementation-Title>
                                                            <Implementation-Version>${project.version}</Implementation-Version>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                    <manifestSection>
                                                        <name>${build.manifest.section}</name>
                                                        <manifestEntries>
                                                            <Build-Time>${maven.build.timestamp}</Build-Time>
                                                            <Build-Number>${build.number}</Build-Number>
                                                            <Build-Revision>${build.revision}</Build-Revision>
                                                        </manifestEntries>
                                                    </manifestSection>
                                                </manifestSections>
                                            </archive>
                                    </configuration>
                            </plugin>
                    </plugins>
                </build>
        </project>
