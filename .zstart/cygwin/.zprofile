# Runs after zshenv//.zshenv//zprofile
# .zprofile is meant as an alternative to .zlogin for ksh fans; .zlogin and .zprofile are NOT mant to be used together (they can be though).
#
# I leave it intentionally blank but for remembering zsh's process.
