CURRDIR=$(pwd)
DIRTY=()

for x in $(find . -type d -depth 1)
do
    cd $x
    if ! git diff-files --quiet --
    then
        DIRTY+=( $x )
    fi
    cd $CURRDIR
done

for d in $DIRTY
do
    echo $d
done

