# Convert an avi or mkv to mp4, not just a simple container switch
[ $# -lt 2 ] && echo "Converts avi or mkv to mp4\n$0 input.xxx output.xxx" && return 1
ffmpeg -i "$1" -acodec libfaac -b:a 128k -vcodec mpeg4 -b:v 1200k -flags +aic+mv4 "$2"
