# Convert an avi or mkv to mp4, not just a simple container switch
[ $# -lt 2 ] && echo "Converts avi or mkv to mp4 without the need of libfaac\n$0 input.xxx output.xxx" && return 1
ffmpeg -i "$1" -c:v libx264 -crf 19 -preset slow -c:a aac -strict experimental -b:a 192k -ac 2 "$2"
