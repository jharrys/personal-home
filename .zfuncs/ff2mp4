# Convert an m4v to mp4, just a simple container switch
[ $# -lt 2 ] && echo "A more complex converter to the mp4 container.\n$0 input.xxx output.xxx" && return 1
ffmpeg -i "$1" -strict experimental -c:v copy -c:a aac -b:a 192k "$2"
