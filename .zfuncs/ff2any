# Convert an m4v to mp4, just a simple container switch
[ $# -lt 2 ] && echo "Simple converter between one container to another container\n$0 input.xxx output.xxx" && return 1
ffmpeg -i "$1" -c copy "$2"
